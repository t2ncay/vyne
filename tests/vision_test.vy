ruleset { warnings, dynamic_casting };

module vmem;

out("--- Vyne Vision System ---");
out("Initial usage: " + string(vmem.usage()) + " bytes");

out("Pəncərə bağlandı.");
out("Final usage: " + string(vmem.usage()) + " bytes");