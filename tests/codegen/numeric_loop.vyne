ruleset { dynamic_casting };

fn sum_squares(n :: Int64) -> Int64 {
    total :: Int64 = 0;
    through i :: 1..n -> loop {
        total = total + i * i;
    };
    return total;
}

out(sum_squares(10));